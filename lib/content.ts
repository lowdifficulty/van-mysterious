import { defaultContent } from "@/lib/default-content";

export const site = defaultContent.site;
export const nav = defaultContent.nav;
export const stills = defaultContent.gallery.stills;
export const experiences = defaultContent.experiences.items;
export const diary = defaultContent.diary.entries;
export const reviews = defaultContent.reviews.items;
export const questions = defaultContent.qa.items;
